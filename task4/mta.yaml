_schema-version: 3.3.0
ID: task4
description: A simple CAP project.
version: 1.0.0
modules:
- name: task4-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: task4-db
  - name: task4-destination
  - name: task4-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: task4-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: task4-db
  parameters:
    buildpack: nodejs_buildpack
- name: task4-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: task4-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - vms.zip
      name: vms
      target-path: resources/
- name: vms
  type: html5
  path: app/vms
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: task4-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: task4-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: GeoCode
            ProxyType: Internet
            Type: HTTP
            URL: https://46aa3ce2trial-dev-task6-srv.cfapps.us10-001.hana.ondemand.com
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-plan: lite
- name: task4-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: task4-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: task4-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: task4-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
